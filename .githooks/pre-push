#!/usr/bin/env bash
set -euo pipefail

while read -r _local_ref _local_sha remote_ref _remote_sha; do
  if [[ "$remote_ref" == "refs/heads/main" ]]; then
    echo "[pre-push] Direct push to main detected."
    echo "[pre-push] Run ./check.sh before pushing if you have not already."
  fi
done
